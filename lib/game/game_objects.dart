import 'dart:ui';

class Enemy {
  Offset position;
  Enemy(this.position);
}

class Goal {
  Offset position;
  Goal(this.position);
}

class Bonus {
  Offset position;
  Bonus(this.position);
}